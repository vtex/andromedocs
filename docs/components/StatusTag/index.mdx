import LiveCode from "../../../components/src/LiveCode";
import StatusTag from "../../../components/src/StatusTag";

# StatusTag

A badge that displays the current status of the component.

## Example

<LiveCode
  scope={{ StatusTag }}
  code={`<div style={{ width: "fit-content" }}>
  <StatusTag variant="stable" />
</div>`}
/>
